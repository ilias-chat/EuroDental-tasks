export const environment = {
  production: true,
  /**
   * Production / `ng build`: set to your real API (e.g. https://eurodental.example/api).
   * For local Electron testing against `php artisan serve`, 127.0.0.1:8000 is fine.
   */
  apiBaseUrl: 'http://127.0.0.1:8000/api',
};
