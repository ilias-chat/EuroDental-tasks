export const environment = {
  production: false,
  /**
   * Match how you run Laravel:
   * - `php artisan serve` → http://127.0.0.1:8000/api (no /public in URL)
   * - XAMPP → e.g. http://localhost/eurodental/public/api
   */
  apiBaseUrl: 'http://127.0.0.1:8000/api',
};
